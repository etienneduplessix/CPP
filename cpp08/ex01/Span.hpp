#ifndef SPAN_HPP
#define SPAN_HPP

#include <stdexcept>
#include <algorithm>
#include <iostream>

class Span {
public:
      using int N ; 
       Span(N);
      Span(const Span &copy);
      virtual ~Span();
       Span& operator=(const Span &assign);
    
private:

};
#endif